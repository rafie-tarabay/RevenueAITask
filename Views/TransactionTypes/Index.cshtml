@model IEnumerable<RevenueAITask.Models.TransactionType>

@{
    ViewData["Title"] = "Index";
}

<h1>Transaction Types</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TransactionTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TransactionTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TransactionTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TransactionTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TransactionTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
